<UserControl
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:LynLogger.Settings" 
             x:Class="LynLogger.Settings.SettingsView" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.DataContext>
        <local:SettingsModel/>
    </UserControl.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto" MinWidth="80"/>
        </Grid.ColumnDefinitions>
        <TextBlock Margin="5,5" Text="基本信息记录间隔" Grid.Column="0" Grid.Row="0" />
        <TextBlock Margin="5,5" Text="舰船信息记录间隔" Grid.Column="0" Grid.Row="1" />
        <TextBox Margin="5,5" Grid.Column="1" Grid.Row="0" Text="{Binding BasicInfoLoggingInterval}"/>
        <TextBox Margin="5,5" Grid.Column="1" Grid.Row="1" Text="{Binding ShipDataLoggingInterval}" />
    </Grid>
</UserControl>
