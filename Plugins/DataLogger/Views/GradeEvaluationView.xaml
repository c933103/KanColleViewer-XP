<UserControl
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:LynLogger.Views" x:Class="LynLogger.Views.GradeEvaluationView" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.DataContext>
        <local:GradeEvaluationModel/>
    </UserControl.DataContext>
    <Grid Margin="10,10,0,0">
        <TextBlock>
            <Run Text="评分：" /><Run Text="{Binding Score, Mode=OneWay}" />
            <LineBreak />
            <Run Text="等级：" /><Run Text="{Binding Grade, Mode=OneWay}" />
        </TextBlock>
    </Grid>
</UserControl>
